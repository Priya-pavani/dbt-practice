version: 2

models:
  - name: bronze_dates #  Must match the filename of a model -- including case sensitivity.
    config:
      materialized: table
  - name: bronze_stores
    config:
      materialized: view
      schema: bronze
    